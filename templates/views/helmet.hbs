<!-- if helmet -->
{{#if helmet}}
	<div class="container">
        <h1>{{helmet.name}}</h1>
	</div>
	<div class="row">
		{{>hero-carousel helmet.gallery}}
		
	</div>

{{else}}
	<!-- else -->
	<h3 class="text-muted">Helmet does not exist.</h3>
{{/if}}
