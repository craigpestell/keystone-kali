<!-- if helmet -->
{{#if helmet}}
	<div class="container">
        <h2>{{helmet.name}}</h2>
	</div>
	<div class="row">
		{{>hero-carousel helmet.gallery}}
		
	</div>

{{else}}
	<!-- else -->
	<h3 class="text-muted">Helmet does not exist.</h3>
{{/if}}
