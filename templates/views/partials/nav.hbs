<nav id="nav-main" class="navbar navbar-static-top" role="navigation">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-collapse-main">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/">
				<div class="navbar-logo" style="background: url({{cloudinaryUrl siteSettings.nav-logo-large.imageValue format="jpg" width=150 }}); background: url({{cloudinaryUrl siteSettings.nav-logo-large.imageValue width=150 }});background-size: 150px 50px;"></div>
            </a>
        </div>
        <div class="collapse navbar-collapse" id="navbar-collapse-main">
        <ul class="nav navbar-nav">
			{{# each navLinks}}
				{{#unless @last}}
					{{#if dropdown}}
						<li class="{{#ifeq key ../../../page}} active{{/ifeq}}"><a href="{{href}}">{{label}}</a></li>
                        <!--<li class="dropdown visible-xs{{#ifeq key ../../../page}} active{{/ifeq}}">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" 
							   aria-haspopup="true" aria-expanded="false">{{label}}<span class="caret"></span></a>
                            <ul class="dropdown-menu">
								{{#each dropdown}}
                                    <li><a href="{{href}}">{{label}}</a></li>
								{{/each}}
                            </ul>
                        </li>-->
					{{else}}
			            <li class="{{#ifeq key ../../../page}} active{{/ifeq}}"><a href="{{href}}">{{label}}</a></li>
					{{/if}}
				{{else}}
		</ul>
        <ul class="nav navbar-nav navbar-right">
            <li class="{{#ifeq key ../../page}} active{{/ifeq}}"><a href="{{href}}">{{label}}</a></li>
			{{#if ../../user}}
				<!--<li><a href="/profile">Profile</a></li>-->
				<li class="dropdown">
					<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
						<img src="http://www.gravatar.com/avatar/fdef5bc02f444aa6d7036004e90ffab1?d=http%3A%2F%2Fsydjs.com%2Fimages%2Favatar.png&amp;r=pg" width="27" class="img-circle">{{user.first}}
					</a>
					<ul class="dropdown-menu">
						<li><a href="/profile" class="user hidden-xs">Profile</a></li>
						<li><a href="/signout">Sign Out</a></li>
					</ul>
				</li>
			{{else}}
				<!--<li><a href="/signin">Sign In</a></li>-->
			{{/if}}
			<!--ul.navbar-nav.navbar-nav--right
					if user
						li: a(href='/me').user.hidden-xs
								img(src=(user.photo.exists ? user._.photo.thumbnail(27,27) : user.avatarUrl || '/images/avatar.png'), width=27).img-circle
								=user.name.first
						li: a(href='/signout')
							span.visible-xs Sign out
							span.entypo.entypo-large.entypo-logout.hidden-xs
					else
						li: a(href='/signin', data-from=page.path, data-initial='signin').js-auth-trigger Sign in
						li: a(href='/join', data-from=page.path).js-auth-trigger Join-->
		</ul>
				{{/unless}}
			{{/each}}
        </div>
    </div>
</nav>
