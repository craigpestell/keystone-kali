{{> blog-category-nav}}
{{#container}}
	{{#ifCond data.posts.totalPages ">" 1}}
	<div class="row blog">
		<div class="col col-xs-3"></div>
	</div>
	{{/ifCond}}
	<div class="row">
		{{> blog-index}}
	</div>

	<div class="row">{{>blog-pagination}}</div>

{{/container}}
